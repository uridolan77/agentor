# Current PR — harness marker

Completed: Phase 37 **PR149–PR153** (observability and operator readiness): **`Agentor.Application.Observability`** primitives (`AgentorEventIds`, `AgentorLogFields`, `SafeLogContext`, `ObservabilityRedaction`, `AgentorCorrelationContext`) + **`IRuntimeMetricsRecorder`**; **`Agentor.Infrastructure.Observability.AgentorRuntimeMetricsRecorder`** (`Agentor.Runtime` meter, `IServiceScopeFactory` for scoped queue/outbox gauges); structured logs + metrics in **`GovernedSingleToolRunDriver`**, **`SequentialAgentPlanExecutor`**, **`RuntimePolicyEvaluator`**, **`ToolExecutionPipeline`**, **`OutboxDispatcher`**, **`RunQueueHostedService`**, **`IntegrationSurfaceService`**; **`CorrelationHeadersDelegatingHandler`** + **`RequestTracingMiddleware`** correlation scope; **`IntegrationHttpError`** appends **`CorrelationId=`** when context set; **`POST /api/v1/agent-runs`** adds **`X-Agentor-Run-Trace-Id`**; **`GET /api/v1/ops/diagnostics-report`** (`OperatorDiagnosticsService`, JSON default + `?format=markdown`); **`docs/operator/observability.md`**, **`docs/OBSERVABILITY.md`**, **`docs/security/AUTHORIZATION_MATRIX.md`**; **`docs/api/openapi-v1.snapshot.json`**; **`AddLogging()`** in **`AddAgentorInfrastructure`**. Harness: **`phase` 37**, **`harnessPass` PR153**; CI **`verify-harness`** ExpectedPhase **37** / **PR153**. Verification: restore/build/test on **`Agentor.sln`** — **544 passed**; **`verify-harness`** ExpectedPhase **37** / **PR153**; **`verify-repo-clean`**.

Next: Phase 38 (security hardening final pass) only when explicitly scheduled.

Do not start the next phase during closeout.
