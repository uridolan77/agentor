# Current PR — harness marker

Completed: Phase 29 **PR120** — **Production auth boundary (ASP.NET + permissions)**: **`AddAuthentication`/`AddJwtBearer`**/**`Agentor.Fake`**/**`Agentor.Header`**/**`Agentor.JwtUnvalidated`** + **`UseAuthentication`/`UseAuthorization`**; **`/api/v1/*`** **`RequireAuthorization(Agentor.Authenticated)`**; **`AgentorPermission`** extended with **`RunRead`/`RunWrite`/`TraceRead`/`QueueRead`/`QueueWrite`/`ManagementRead`/`ManagementWrite`**; **`EndpointAuthorization`** on **`AgentRunEndpoints`**, **`RunQueueEndpoints`**, **`AthanorEndpoints`**, **`Phase13ProductEndpoints`** (management + run aliases); **`GET /ready`** and **`GET /api/v1/integrations/status`** authenticated (**`OpsRead`** on integrations); **`AgentorAuthOptions`**: **`JwtAuthority`**, **`JwtAudience`**, **`JwtAcceptUnvalidatedBearerTokens`**; **`AgentorAuthOptionsValidator`** requires authority or unvalidated flag for Jwt mode; **`RoleBasedAuthorizationDecisionService`** service-role read matrix; docs **`docs/security/auth-boundary.md`**, **`docs/security/AUTHORIZATION_MATRIX.md`**, **`docs/REPO_TRUTH.md`**; tests **`Phase29WebAuthenticationApiTests`**, **`RoleBasedAuthorizationDecisionServiceTests`**, **`AgentorAuthOptionsValidatorTests`**. Verification: restore/build/test — **456 passed**; harness scripts **ExpectedPhase 29 / PR120**.

Next: Phase 30 (structured tool I/O v2) or next explicitly scheduled phase.

Do not start the next phase during closeout.
