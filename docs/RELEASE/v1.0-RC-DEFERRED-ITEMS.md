# v1.0 RC — Deferred acceptance items

This manifest reflects `.agentor-harness/feature-list.json` rows where `passes` is false. It is a triage aid, not a removal of harness rows.

## Engineering notes

- **PR23-API-003 / PR24-API-003** were closed in **PR75.8** with `WebApplicationFactory` + in-memory repository seeding a **Running** `AgentRun` (`tests/Agentor.Api.Tests/AthanorRunningRunApiTests.cs`).
- **PR52-004** was closed in **Phase 17 (PR81–PR85)** by implementing the versioned `PolicyBundle` domain model, `PolicyBundleRulesAdapter`, management API, evaluation adapter, and audit identity export. See `docs/developer/policy-bundles.md`.
- **PR53-005** was closed in **Phase 18 (PR86–PR90)** by `PlanResumeCursor`, mid-plan suspension recording, approval-driven multi-step resume, and named fixture evidence in `tests/Agentor.Application.Tests/Phase18FixtureTests.cs`.
- **SCOPE-001** was closed in **Phase 26 (PR117)** — `PolicyBundleRulesAdapter.ToProfileRules(bundle, AgentRunScope)` filters rules by run identity; `PolicyEvaluationRequest` carries `AgentRunScope`; precedence documented in `docs/developer/policy-bundles.md`. Evidence: `PolicyScopeEvaluationTests`, `PolicyBundleRulesAdapter`.

## Acceptance items (`passes: false`)

Count: 0

There are **no** active deferred harness rows at this revision. Historical deferred narratives above remain for traceability.

PR53-005 is no longer deferred. Named closure evidence: `Phase18FixtureTests.PR53_005_Evidence_ThreeStepPlan_ReviewAtS2_ResumedAndCompletedByApproval`, `MultiStepReviewResumeTests`, and the Phase 18 audit fixtures.
