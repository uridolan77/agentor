# v1.0 RC — Deferred acceptance items

This manifest reflects `.agentor-harness/feature-list.json` rows where `passes` is false. It is a triage aid, not a removal of harness rows.

## Engineering notes from repository tightening

- **Program.cs** was split into `src/Agentor.Api/Endpoints/*.cs` in **PR75.7** (behavior-preserving wiring).
- **PR23-API-003 / PR24-API-003** still require a **running-run API fixture** (e.g. `WebApplicationFactory` + in-memory repository seeding a `Running` run) to assert 2xx on the public Athanor-shaped paths while the run is active. No product behavior was added in PR75.7 solely to flip these rows.

## Acceptance items (`passes: false`)

Count: 4

### PR23-API-003

- **Description:** Public API returns 2xx for evidence-provenance while run is `Running`
- **Evidence / TODO:** Needs `WebApplicationFactory` (or equivalent) test with a run left in `Running` — not added in PR75.7
- **Proposed disposition:** **Needs test fixture only** (no API contract change required for the check itself)
- **Rationale:** Harness row stays false until a named test or source path documents the success path with a running run

### PR24-API-003

- **Description:** Public API returns 2xx for candidate submit while run is `Running`
- **Evidence / TODO:** Same running-run fixture gap as PR23-API-003
- **Proposed disposition:** **Needs test fixture only** (shared fixture as PR23)
- **Rationale:** Same as PR23 — acceptance needs explicit API test evidence

### PR52-004

- **Description:** Versioned PolicyBundle / full enterprise policy engine (deferred beyond `PolicyProfileRules`)
- **Evidence / TODO:** Not implemented beyond profile rules; harness documents partial coverage
- **Proposed disposition:** **Defer to v1.1** / enterprise policy model
- **Rationale:** Out of scope for v1.0 RC repository tightening

### PR53-005

- **Description:** Multi-step plan executor resume semantics beyond the single pending-tool path
- **Evidence / TODO:** Requires dedicated executor tests and design sign-off
- **Proposed disposition:** **Defer to v1.1** / multi-step resume semantics
- **Rationale:** Not addressed in Phase 15 cleanup passes
