# Agentor v1.0 release candidate notes

This document describes **RC readiness** (reviewable, test-gated, documented boundaries). It is **not** a claim of full production hardening for every deployment model.

## Scope

Phase 15 closes platform hardening PR71–PR75: redaction hygiene, performance baseline harness, CI release pipeline checks, migration readiness documentation, and RC boundary review.

## Quality gates

- `dotnet test Agentor.sln` green (all test projects).
- Docker image builds (`docker build -t agentor:ci .`).
- EF migrations enumerated (`dotnet ef migrations list`).
- Benchmark harness project builds Release (timings are **local**; see `docs/developer/phase15-performance-baselines.md`).

## Deferred / watchlist

- Items still `passes: false` in `.agentor-harness/feature-list.json` are listed with rationale in `docs/RELEASE/v1.0-RC-DEFERRED-ITEMS.md` and remain **explicitly deferred** until evidenced.

## Next after RC

Post-RC work follows roadmap phases after Phase 15 (only when explicitly scheduled).